@inject IViewLocalizer Localizer
@model ProjectDetailViewModel

@{
    ViewData["Title"] = @Model.Title;
}

<div class="project-container">
    <div class="text-md-start justify-content-lg-start mb-5">
        <h1 class="text-center border-bottom pb-2 mb-4">@Model.Title</h1>
        <div class="row">
            <!-- Column Left -->
            <div class="col-md-4">
                <div class="card">
                    @if (Model.Skills != null)
                    {
                        <div class="card-body">
                            <h5 class="card-title">@Localizer["Skills"]</h5>
                            <ul class="list-group list-group-flush">
                                @foreach (string skill in Model.Skills)
                                {
                                    <li class="list-group-item">@skill</li>
                                }
                            </ul>
                        </div>
                    }
                    @if (Model.GithubUrl != null || Model.ProjectUrl != null)
                    {
                        <div class="card-footer">
                            @if (Model.GithubUrl != null)
                            {

                                <a class="btn btn-md btn-outline-primary m-2" href="@Model.GithubUrl">
                                    <i class="bi bi-github"></i> View Code
                                </a>
                            }
                            @if (Model.ProjectUrl != null)
                            {
                                <a class="btn btn-md btn-outline-primary m-2" href="@Model.ProjectUrl">
                                    <i class="bi bi-arrow-up-right-square-fill"></i> @Model.Title
                                </a>
                            }
                        </div>
                    }                    
                </div>
            </div>

            <!-- Column Right -->
            <div class="col-md-7">
                <p class="text-md-start multiline-text">@Model.Description</p>
            </div>
        </div>
    </div>

    <h1 class="display-4 text-center">Gallery</h1>
    @if (Model.Pictures.Count > 0)
    {
        <div class="carousel-wrapper">
            <div id="CustomCarousel" class="carousel slide" data-bs-ride="carousel">
            
                <div class="carousel-inner">
                    @foreach (var picture in Model.Pictures)
                    {
                        <div class="carousel-item @(picture == Model.Pictures.First() ? "active" : "")">
                            <img class="d-block w-100" src="@picture" alt="@Model.Title">
                        </div>
                    }
                </div>
                <button class="carousel-control-prev" type="button" data-bs-target="#CustomCarousel" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon"></span>
                </button>

                <button class="carousel-control-next" type="button" data-bs-target="#CustomCarousel" data-bs-slide="next">
                    <span class="carousel-control-next-icon"></span>
                </button>
            </div>
        </div>
    }

</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
}